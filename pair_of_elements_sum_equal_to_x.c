#include <stdio.h>
int main() {
   int a[]={1,2,3,4,5};
   int x=9;
   for(int i=0;i<n;i++)
   {
   	for(int j=i;j<n;j++)
   	{
   		if((a[i]+a[j]==x))
   		{
   			printf("the elements are %d %d and they are at %d %d indexes ",a[i],a[j],i,j);
		   }
	   }
	} 
return 0;
}
